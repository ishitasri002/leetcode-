class Solution(object):
    def moveZeroes(self, nums):
        n = len(nums)
        start = 0
        for i in range (n):
            if nums[i] != 0:
                nums[start] = nums[i]
                start +=1

        for i in range(start,n):
            nums[i] = 0

 
